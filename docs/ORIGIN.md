# Origin

This repo started as an offline analyzer and CLI for Microsoft Intune `managedDevices` exports.

It was then expanded into a public operator surface so the Microsoft admin lane is visible in portfolio form:

- Intune device compliance
- stale endpoint sync drift
- BYOD governance
- encryption posture
- remediation readiness

No live tenant credentials or production Graph exports are stored in the repo.
